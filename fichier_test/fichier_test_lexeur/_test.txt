# Test 1: Commentaires et indentation basique
def test1():
    # Ceci est un commentaire
    x = 42  # Commentaire en fin de ligne
    if x > 0:
        print("positif")
    else:
        print("négatif")
# Test 3: Nombres et opérateurs
def calculateur():
    a = 0
    b = 123
    c = 9999
    resultat = ((a + b) * c) // 2
    test = a <= b and b >= c or not c == 0
    return resultat
# Test 5: Boucle for et mots-clés
def test_boucle():
    nombres = [1, 2, 3]
    for x in nombres:
        if True:
            print(x)
        else:
            return None
    return True
# Test 6: Indentations multiples et opérateurs
def indentation_complexe():
    x = 1
    if x > 0:
        if x < 10:
            if x != 5:
                print("niveau 3")
            else:
                print("else niveau 3")
        else:
            print("else niveau 2")
    else:
        print("else niveau 1")
# Test 7: Tous les opérateurs
def test_operateurs():
    a = 5
    b = 3
    c = a + b    # addition
    c = a - b    # soustraction
    c = a * b    # multiplication
    c = a // b   # division entière
    c = a % b    # modulo
    c = -a       # moins unaire
    c = a < b    # plus petit
    c = a <= b   # plus petit ou égal
    c = a > b    # plus grand
    c = a >= b   # plus grand ou égal
    c = a == b   # égal
    c = a != b   # différent
    b = not a    # négation
    b = a and b  # et logique
    b = a or b   # ou logique
# Test 8: Mix de tout
def programme_complet():
    # Initialisation
    _compteur = 0
    resultat123 = None
    if True and not False:
        texte = "Test\n\"complexe\""
        for i in [1, 2, 3]:
            _compteur = _compteur + i
            if i <= 2:
                print(i)
            else:
                return None
    return True
# Test 2: Identifiants variés
variable = 0
_variable = 1
variable_1 = 2
Variable_Test = 3
_2variable = 4
# Test 4: Chaînes de caractères avec échappement
message1 = "Test des \"guillemets\""
message2 = "Ligne 1\nLigne 2"